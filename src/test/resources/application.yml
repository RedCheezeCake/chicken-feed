SERVER:
  PORT: 9090

spring:
  jpa:
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:h2:mem:test
    username: root
    password: password
  flyway:
    enabled: false

ticket:
  stream:
    size:
      init: 10
      maximum: 10

logging:
  level:
    me:
      donald:
        chickenfeed: debug